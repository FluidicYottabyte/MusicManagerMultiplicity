<Window x:Class="MusicManagerMultiplicity.CreatePlaylistDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicManagerMultiplicity"
        mc:Ignorable="d"
        Title="Create Playlist" Height="400" Width="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <DockPanel>
                    <Label>Playlist Name:</Label>
                    <TextBox VerticalContentAlignment="Center" HorizontalAlignment="Stretch" Margin="3,5,5,5">Enter Playlist Name</TextBox>
                </DockPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <RibbonCheckBox Label="Enable Image"/>
                    <Button Margin="3,5" Width="Auto">Select Image</Button>
                    <TextBlock VerticalAlignment="Center" Margin="5,2" TextWrapping="Wrap" Width="Auto" Text="Impliment me!"/>
                </StackPanel>
            </StackPanel>

            <Grid Grid.Row="1" VerticalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>


                <ListBox Grid.Row="1" VerticalAlignment="Stretch" Margin="3,5,5,5">
                    <ListBoxItem>Test</ListBoxItem>
                </ListBox>
            </Grid>
        </Grid>


        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5">
            <Button Tag="{Binding CreatePlaylistName}" HorizontalAlignment="Center" Click="CreatePlaylist" Margin="5,0">Done</Button>
            <Button Tag="{Binding CancelButton}" HorizontalAlignment="Center" Click="CancelPlaylistCollection" Margin="5,0">Cancel</Button>
        </StackPanel>


        <Grid Grid.Column="1" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <TextBox Grid.Row="1" Margin="3,5">Search Library</TextBox>

            <ListBox Grid.Row="2" VerticalAlignment="Stretch" Name="LibrarySearchBox"  Margin="3,5">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Orientation="Horizontal" Grid.Column="0">
                                <Image x:Name="PlaylistArt" Width="25" Source="{Binding ImageSource}"/>
                                <TextBlock Text="{Binding SongName}" Margin="8, 5" FontSize="17" FontFamily="Times New Roman" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Button Tag="{Binding PlayButtonName}" HorizontalAlignment="Right" Click="AddSelectedSong" Margin="5,0">Add</Button>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>

        </Grid>
    </Grid>
</Window>
