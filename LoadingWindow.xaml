<Window x:Class="MusicManagerMultiplicity.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicManagerMultiplicity"
        mc:Ignorable="d"
        Title="Loading" Height="100" Width="200"
        WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Topmost="True" AllowsTransparency="True" Background="White">
    <!-- LoadingWindow.xaml -->
	<Grid Background="White" Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
		
		<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Background="White">
			<TextBlock Text="Loading songs..." FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Center"/>
			<ProgressBar x:Name="progressBar" Minimum="0" Maximum="100" Width="160" Height="10"/>
		</StackPanel>
	</Grid>


</Window>
